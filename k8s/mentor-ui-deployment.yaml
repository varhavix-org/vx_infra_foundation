apiVersion: apps/v1
kind: Deployment
metadata:
  name: vx-mentor-ui
  namespace: varhavix
  labels:
    app: vx-mentor-ui
    tier: ui
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vx-mentor-ui
  template:
    metadata:
      labels:
        app: vx-mentor-ui
        tier: ui
    spec:
      containers:
        - name: vx-mentor-ui
          image: varhavix/vx-mentor-ui:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: vx-mentor-ui
  namespace: varhavix
spec:
  selector:
    app: vx-mentor-ui
  ports:
    - port: 80
      targetPort: 80
