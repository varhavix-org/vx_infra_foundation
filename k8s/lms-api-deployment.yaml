apiVersion: apps/v1
kind: Deployment
metadata:
  name: vx-lms-api
  namespace: varhavix
  labels:
    app: vx-lms-api
    tier: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vx-lms-api
  template:
    metadata:
      labels:
        app: vx-lms-api
        tier: api
    spec:
      containers:
        - name: vx-lms-api
          image: varhavix/vx-lms-api:latest
          ports:
            - containerPort: 8081
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: vx-lms-api
  namespace: varhavix
spec:
  selector:
    app: vx-lms-api
  ports:
    - port: 8081
      targetPort: 8081
