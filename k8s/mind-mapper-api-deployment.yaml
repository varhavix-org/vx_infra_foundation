apiVersion: apps/v1
kind: Deployment
metadata:
  name: vx-mind-mapper-api
  namespace: varhavix
  labels:
    app: vx-mind-mapper-api
    tier: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vx-mind-mapper-api
  template:
    metadata:
      labels:
        app: vx-mind-mapper-api
        tier: api
    spec:
      containers:
        - name: vx-mind-mapper-api
          image: varhavix/vx-mind-mapper-api:latest
          ports:
            - containerPort: 8082
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: vx-mind-mapper-api
  namespace: varhavix
spec:
  selector:
    app: vx-mind-mapper-api
  ports:
    - port: 8082
      targetPort: 8082
