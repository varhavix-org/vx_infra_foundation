apiVersion: apps/v1
kind: Deployment
metadata:
  name: vx-media-ui
  namespace: varhavix
  labels:
    app: vx-media-ui
    tier: ui
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vx-media-ui
  template:
    metadata:
      labels:
        app: vx-media-ui
        tier: ui
    spec:
      containers:
        - name: vx-media-ui
          image: varhavix/vx-media-ui:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: vx-media-ui
  namespace: varhavix
spec:
  selector:
    app: vx-media-ui
  ports:
    - port: 80
      targetPort: 80
