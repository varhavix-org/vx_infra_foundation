apiVersion: apps/v1
kind: Deployment
metadata:
  name: vx-admin-ui
  namespace: varhavix
  labels:
    app: vx-admin-ui
    tier: ui
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vx-admin-ui
  template:
    metadata:
      labels:
        app: vx-admin-ui
        tier: ui
    spec:
      containers:
        - name: vx-admin-ui
          image: varhavix/vx-admin-ui:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: vx-admin-ui
  namespace: varhavix
spec:
  selector:
    app: vx-admin-ui
  ports:
    - port: 80
      targetPort: 80
