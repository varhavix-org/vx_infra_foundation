apiVersion: apps/v1
kind: Deployment
metadata:
  name: vx-admin-api
  namespace: varhavix
  labels:
    app: vx-admin-api
    tier: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vx-admin-api
  template:
    metadata:
      labels:
        app: vx-admin-api
        tier: api
    spec:
      containers:
        - name: vx-admin-api
          image: varhavix/vx-admin-api:latest
          ports:
            - containerPort: 8084
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: vx-admin-api
  namespace: varhavix
spec:
  selector:
    app: vx-admin-api
  ports:
    - port: 8084
      targetPort: 8084
